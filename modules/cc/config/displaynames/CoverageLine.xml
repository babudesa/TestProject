<?xml version="1.0"?>
<Entity
  name="CoverageLine">
  <Columns>
    <Column
      beanPath="CoverageLine.CoverageSubtype"
      name="subtype"
      sortOrder="1"/>
  </Columns>
  <DisplayName><![CDATA[var retString = "";
      if (subtype == "PropertyCoverage") {
        retString = displaykey.DisplayName.Coverage.Property;
      }
      if (subtype == "VehicleCoverage") {
        retString = displaykey.DisplayName.Coverage.Vehicle;
      }
      if (subtype == "PolicyCoverage") {
        retString = displaykey.DisplayName.Coverage.Policy;
      }
      return retString;]]></DisplayName>
</Entity>
