<?xml version="1.0"?>
<plugin
  name="GeocodePlugin">
  <plugin-gosu
    disabled="true"
    gosuclass="gw.plugin.geocode.impl.BingMapsPlugin">
    <!-- required parameters  credentials -->
    <param
      name="applicationKey"
      value=""/>
    <!-- optional parameters  map url  width and height are expressed in pixels -->
    <param
      name="mapUrlWidth"
      value=""/>
    <param
      name="mapUrlHeight"
      value=""/>
  </plugin-gosu>
</plugin>
