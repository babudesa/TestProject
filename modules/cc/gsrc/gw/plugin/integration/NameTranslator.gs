package gw.plugin.integration

uses gw.api.util.mapping.IFieldMapper
uses gw.lang.reflect.IPropertyInfo

/**
 * Interface used for translating names between two GWObject types.
 */
interface NameTranslator extends gw.api.util.mapping.NameTranslator {
}
