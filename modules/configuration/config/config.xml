<?xml version="1.0"?>

<config xmlns="http://guidewire.com/config">

  <!-- Registry element definition  -->
  <registry>
  	<systemproperty name="env" value="gw.cc.env" default="local"/>
	 <server serverid="cvglq13701.td.afg" env="cert" isbatchserver="true"/>
	 <server serverid="cvglq13702.td.afg" env="cert" isbatchserver="false"/>
	 <server serverid="cvglq13703.td.afg" env="cert" isbatchserver="false"/>
	 <server serverid="cvglp13701.td.afg" env="prod" isbatchserver="true"/>
	 <server serverid="cvglp13702.td.afg" env="prod" isbatchserver="false"/>  	
	 <server serverid="cvglp13703.td.afg" env="prod" isbatchserver="false"/>
  </registry>

  <!--
  ==============================================================================
    Database Configuration

    NOTES:
     - Mappings of logical to physical tablespace names are required for Oracle
  ==============================================================================
  -->
  
    <!-- H2 (meant for demo/quickstart use only!) -->
 <!-- <database name="ClaimCenterDatabase" driver="dbcp" dbtype="h2" autoupgrade="true" checker="false" printcommands="false">
   <param name="jdbcURL" value="jdbc:h2:file:/tmp/guidewire/cc"/>
   <param name="jdbcURLtest" value="jdbc:h2:file:/tmp/guidewire/cctest"/>
   <param name="stmtPool.enabled" value="false"/>
   <param name="maxWait" value="30000"/>
   <archive name="archive1" driver="dbcp" reference="L">
     <param name="jdbcURL" value="jdbc:h2:file:/tmp/guidewire/ccarchive"/>
     <param name="jdbcURLtest" value="jdbc:h2:file:/tmp/guidewire/ccarchivetest"/>
   </archive>
  </database>
-->

  <!-- MS SQL Server -->
   <database name="ClaimCenterDatabase" driver="dbcp"
    dbtype="sqlserver" autoupgrade="true" checker="true" env="local">
    <param name="jdbcURL"
      value="jdbc:sqlserver://localhost:1433;DatabaseName=ClaimCenter6;User=sa;Password=123" />
    <param name="stmtPool.enabled" value="false" />
    <param name="maxWait" value="30000" />
    <param name="checker.threads" value="1" />
    <upgrade collectstorageinstrumentation="false" sqlserverCreateIndexSortInTempDB="true">
      <versiontriggers dbmsperfinfothreshold="600" />
    </upgrade>
    </database>
  <!-- MS SQL Server Upgrade Dev-->
    <database name="ClaimCenterDatabase" driver="dbcp"
    dbtype="sqlserver" autoupgrade="true" checker="true" env="dev">
    <param name="jdbcURL"
      value="jdbc:sqlserver://CVGWDCLU07:1435;DatabaseName=ClaimCenter_dev;User=${username};Password=${password}" />
    <param name="stmtPool.enabled" value="false" />
    <param name="maxWait" value="30000" />
    <param name="checker.threads" value="1" />
    <upgrade collectstorageinstrumentation="false" sqlserverCreateIndexSortInTempDB="true">
      <versiontriggers dbmsperfinfothreshold="600" />
    </upgrade>
    </database>
    <!-- MS SQL Server Upgrade Dev2-->
    <database name="ClaimCenterDatabase" driver="dbcp"
    dbtype="sqlserver" autoupgrade="true" checker="true" env="dev2">
    <param name="jdbcURL"
      value="jdbc:sqlserver://CVGWDCLU07:1435;DatabaseName=ClaimCenter_dev2;User=${username};Password=${password}" />
    <param name="stmtPool.enabled" value="false" />
    <param name="maxWait" value="30000" />
    <param name="checker.threads" value="1" />
    <upgrade collectstorageinstrumentation="false" sqlserverCreateIndexSortInTempDB="true">
      <versiontriggers dbmsperfinfothreshold="600" />
    </upgrade>
    </database>
  <!-- MS SQL Server Upgrade Dev3-->
    <database name="ClaimCenterDatabase" driver="dbcp"
    dbtype="sqlserver" autoupgrade="true" checker="true" env="dev3">
    <param name="jdbcURL"
      value="jdbc:sqlserver://CVGWDCLU07:1435;DatabaseName=ClaimCenter_dev3;User=${username};Password=${password}" />
    <param name="stmtPool.enabled" value="false" />
    <param name="maxWait" value="30000" />
    <param name="checker.threads" value="1" />
    <upgrade collectstorageinstrumentation="false" sqlserverCreateIndexSortInTempDB="true">
      <versiontriggers dbmsperfinfothreshold="600" />
    </upgrade>
    </database>
  <!-- MS SQL Server Upgrade Dev4-->
    <database name="ClaimCenterDatabase" driver="dbcp"
    dbtype="sqlserver" autoupgrade="true" checker="true" env="dev4">
    <param name="jdbcURL"
      value="jdbc:sqlserver://CVGWDCLU07:1435;DatabaseName=ClaimCenter_dev4;User=${username};Password=${password}" />
    <param name="stmtPool.enabled" value="false" />
    <param name="maxWait" value="30000" />
    <param name="checker.threads" value="1" />
    <upgrade collectstorageinstrumentation="false" sqlserverCreateIndexSortInTempDB="true">
      <versiontriggers dbmsperfinfothreshold="600" />
    </upgrade>
    </database>
  <!-- MS SQL Server Upgrade Dev5-->
    <database name="ClaimCenterDatabase" driver="dbcp"
    dbtype="sqlserver" autoupgrade="true" checker="true" env="dev5">
    <param name="jdbcURL"
      value="jdbc:sqlserver://CVGWDCLU07:1435;DatabaseName=ClaimCenter_dev5;User=${username};Password=${password}" />
    <param name="stmtPool.enabled" value="false" />
    <param name="maxWait" value="30000" />
    <param name="checker.threads" value="1" />
    <upgrade collectstorageinstrumentation="false" sqlserverCreateIndexSortInTempDB="true">
      <versiontriggers dbmsperfinfothreshold="600" />
    </upgrade>
    </database>
  <!-- MS SQL Server Upgrade Dev6-->
    <database name="ClaimCenterDatabase" driver="dbcp"
    dbtype="sqlserver" autoupgrade="true" checker="true" env="dev6">
    <param name="jdbcURL"
      value="jdbc:sqlserver://CVGWDCLU07:1435;DatabaseName=ClaimCenter_dev6;User=${username};Password=${password}" />
    <param name="stmtPool.enabled" value="false" />
    <param name="maxWait" value="30000" />
    <param name="checker.threads" value="1" />
    <upgrade collectstorageinstrumentation="false" sqlserverCreateIndexSortInTempDB="true">
      <versiontriggers dbmsperfinfothreshold="600" />
    </upgrade>
    </database>
  <!-- MS SQL Server Upgrade Dev7-->
    <database name="ClaimCenterDatabase" driver="dbcp"
    dbtype="sqlserver" autoupgrade="true" checker="true" env="dev7">
    <param name="jdbcURL"
      value="jdbc:sqlserver://CVGWDCLU07:1435;DatabaseName=ClaimCenter_dev7;User=${username};Password=${password}" />
    <param name="stmtPool.enabled" value="false" />
    <param name="maxWait" value="30000" />
    <param name="checker.threads" value="1" />
    <upgrade collectstorageinstrumentation="false" sqlserverCreateIndexSortInTempDB="true">
      <versiontriggers dbmsperfinfothreshold="600" />
    </upgrade>
    </database>
  <!-- MS SQL Server Upgrade Dev8-->
    <database name="ClaimCenterDatabase" driver="dbcp"
    dbtype="sqlserver" autoupgrade="true" checker="true" env="dev8">
    <param name="jdbcURL"
      value="jdbc:sqlserver://CVGWDCLU07:1435;DatabaseName=ClaimCenter_dev8;User=${username};Password=${password}" />
    <param name="stmtPool.enabled" value="false" />
    <param name="maxWait" value="30000" />
    <param name="checker.threads" value="1" />
    <upgrade collectstorageinstrumentation="false" sqlserverCreateIndexSortInTempDB="true">
      <versiontriggers dbmsperfinfothreshold="600" />
    </upgrade>
    </database>
  <!-- MS SQL Server Upgrade Dev9-->
    <database name="ClaimCenterDatabase" driver="dbcp"
    dbtype="sqlserver" autoupgrade="true" checker="true" env="dev9">
    <param name="jdbcURL"
      value="jdbc:sqlserver://CVGWDCLU07:1435;DatabaseName=ClaimCenter_dev9;User=${username};Password=${password}" />
    <param name="stmtPool.enabled" value="false" />
    <param name="maxWait" value="30000" />
    <param name="checker.threads" value="1" />
    <upgrade collectstorageinstrumentation="false" sqlserverCreateIndexSortInTempDB="true">
      <versiontriggers dbmsperfinfothreshold="600" />
    </upgrade>
    </database>
    <!-- MS SQL Server Upgrade Int-->
    <database name="ClaimCenterDatabase" driver="dbcp"
    dbtype="sqlserver" autoupgrade="true" checker="true" env="int">
    <param name="jdbcURL"
      value="jdbc:sqlserver://CVGWDCLU07:1435;DatabaseName=ClaimCenter_int;User=${username};Password=${password}" />
    <param name="stmtPool.enabled" value="false" />
    <param name="maxWait" value="30000" />
    <param name="checker.threads" value="1" />
    <upgrade collectstorageinstrumentation="false" sqlserverCreateIndexSortInTempDB="true">
      <versiontriggers dbmsperfinfothreshold="600" />
    </upgrade>
    </database>
    
    <!-- MS SQL Server Upgrade Int2-->
    <database name="ClaimCenterDatabase" driver="dbcp"
    dbtype="sqlserver" autoupgrade="true" checker="true" env="int2">
    <param name="jdbcURL"
      value="jdbc:sqlserver://CVGWDCLU07:1435;DatabaseName=ClaimCenter_int2;User=${username};Password=${password}" />
    <param name="stmtPool.enabled" value="false" />
    <param name="maxWait" value="30000" />
    <param name="checker.threads" value="1" />
    <upgrade collectstorageinstrumentation="false" sqlserverCreateIndexSortInTempDB="true">
      <versiontriggers dbmsperfinfothreshold="600" />
    </upgrade>
    </database>
    
    <!-- MS SQL Server Upgrade Int3-->
    <database name="ClaimCenterDatabase" driver="dbcp"
    dbtype="sqlserver" autoupgrade="true" checker="true" env="int3">
    <param name="jdbcURL"
      value="jdbc:sqlserver://CVGWDCLU07:1435;DatabaseName=ClaimCenter_int3;User=${username};Password=${password}" />
    <param name="stmtPool.enabled" value="false" />
    <param name="maxWait" value="30000" />
    <param name="checker.threads" value="1" />
    <upgrade collectstorageinstrumentation="false" sqlserverCreateIndexSortInTempDB="true">
      <versiontriggers dbmsperfinfothreshold="600" />
    </upgrade>
    </database>
    
    
    <!-- MS SQL Server Upgrade Cert-->
    <database name="ClaimCenterDatabase" driver="dbcp"
    dbtype="sqlserver" autoupgrade="true" checker="true" env="cert">
    <param name="jdbcURL"
      value="jdbc:sqlserver://CVGWQCLU07DB:1435;DatabaseName=ClaimCenter_cert;User=${username};Password=${password}" />
    <param name="stmtPool.enabled" value="false" />
    <param name="maxWait" value="30000" />
    <param name="checker.threads" value="1" />
    <upgrade collectstorageinstrumentation="false" sqlserverCreateIndexSortInTempDB="true">
      <versiontriggers dbmsperfinfothreshold="600" />
    </upgrade>
    </database>
    <!-- MS SQL Server Upgrade UAT-->
    <database name="ClaimCenterDatabase" driver="dbcp"
    dbtype="sqlserver" autoupgrade="true" checker="true" env="uat">
    <param name="jdbcURL"
      value="jdbc:sqlserver://CVGWDCLU07:1435;DatabaseName=ClaimCenter_uat;User=${username};Password=${password}" />
    <param name="stmtPool.enabled" value="false" />
    <param name="maxWait" value="30000" />
    <param name="checker.threads" value="1" />
    <upgrade collectstorageinstrumentation="false" sqlserverCreateIndexSortInTempDB="true">
      <versiontriggers dbmsperfinfothreshold="600" />
    </upgrade>
    </database>
    <!-- MS SQL Server Upgrade Prod-->
    <database name="ClaimCenterDatabase" driver="dbcp"
    dbtype="sqlserver" autoupgrade="true" checker="true" env="prod">
    <param name="jdbcURL"
      value="jdbc:sqlserver://CVGWPCLU07DB:1435;DatabaseName=ClaimCenter;User=${username};Password=${password}" />
    <param name="stmtPool.enabled" value="false" />
    <param name="maxWait" value="30000" />
    <param name="checker.threads" value="1" />
    <upgrade collectstorageinstrumentation="false" sqlserverCreateIndexSortInTempDB="true">
      <versiontriggers dbmsperfinfothreshold="600" />
    </upgrade>
    </database>

  <!-- ==============================================================================
   Environment Parameters
  ==============================================================================
   The prefix to use for publicids generated by the application.  Generated
       publicids will be of the form prefix:id where id is the actual id for the
       entity.  This parameter should be set to different values in production
       and test systems to allow for the clean import/export of data between
       systems. This field must be 9 characters or less in length. -->
  <param env="local" name="PublicIDPrefix" value="cclu"/>
  <param env="dev" name="PublicIDPrefix" value="ccd1u1"/>
  <param env="dev2" name="PublicIDPrefix" value="ccd2u1"/>
  <param env="dev3" name="PublicIDPrefix" value="ccd3u"/>
  <param env="dev4" name="PublicIDPrefix" value="ccd4u"/>
  <param env="dev5" name="PublicIDPrefix" value="ccd5u"/>
  <param env="dev6" name="PublicIDPrefix" value="ccd6u"/>
  <param env="dev7" name="PublicIDPrefix" value="ccd7u"/>
  <param env="dev8" name="PublicIDPrefix" value="ccd8u"/>
  <param env="dev9" name="PublicIDPrefix" value="ccd9u"/>
  <param env="int" name="PublicIDPrefix" value="ccint1u1"/>
  <param env="int2" name="PublicIDPrefix" value="ccint2u"/>
  <param env="int3" name="PublicIDPrefix" value="ccint3u"/>
  <param env="uat" name="PublicIDPrefix" value="utcc"/>
  <param env="cert" name="PublicIDPrefix" value="ctccu1"/>
  <param name="PublicIDPrefix" value="cc"/>
  <!-- Enable internal debug tools page http://localhost:8080/app/InternalTools.do -->
  <param env="prod" name="EnableInternalDebugTools" value="false"/>
  <param name="EnableInternalDebugTools" value="true"/>
  <!-- The default application locale if locale can't be determined from user
       or browser -->
  <param name="DefaultApplicationLocale" value="en_US"/>
  <!-- Denotes the username of the unrestricted user.  If not specified,
       defaults to "su" -->
  <param name="UnrestrictedUserName" value="su"/>
  <!-- Number of minutes between each memory usage monitor logging. -->
  <param name="MemoryUsageMonitorIntervalMins" value="15"/>
  <!-- Indicates if resoruces are mutable on this server. Studio uses this to
       determine whether or not to enable modification of resources. -->
  <param name="ResourcesMutable" value="true"/>
  <!-- Whether or not the GScript compiler generates errors if it detects
       unreachable code or missing return statements. -->
  <param name="UnreachableCodeDetection" value="true"/>

  <!-- Whether or not the Gosu compiler generates warnings if an implicit coercion is occuring in a program. -->
  <param name="WarnOnInplicitCoercion" value="true"/>


 <!-- Whether to use the old-style pre-update framework or to use the new gosu class framework.
      If true, the validation graph will be used to determine what beans should have their pre-update rules run.
      If false, the PreUpdate gosu class will be invoked -->
  <param name="UseOldStylePreUpdate" value="true"/>

  <!-- The default country that will be used when an address does not have a
       country explicitly set.  This is also used as the default for new
       addresses that are created. This is a Country typecode, which normally
       correspond to the standard ISO two-letter codes. -->
  <param name="DefaultCountryCode" value="US"/>

   <!-- The threshold year value for determining whether to resolve a two-digit year to the earlier or later century. -->
  <!--param name="TwoDigitYearThreshold" value="50"/-->

   <!-- Enable to provide address verification warnings -->
  <param name="EnableAddressVerification" value="false"/>

  <!-- Flag to indicate that Address validation failure should be errors rather 
      than warnings -->
  <param name="AddressVerificationFailureAsError" value="false"/>

  <!-- The location of the web resources directory under the root of the tomcat config directory. -->
  <param name="WebResourcesDir" value="resources"/>
    
  <!-- ==============================================================================
    Security
  ============================================================================== -->
  <security sessiontimeoutsecs="7200"/>
  <!-- The minimum length of a user's password. -->
  <param name="MinPasswordLength" value="2"/>
  <!-- The maximum length of a user's password. -->
  <param name="MaxPasswordLength" value="16"/>
  <!-- Number of failed attempts that are allowed before a user is locked out,
       or -1 to disable the account lockout feature. For example, setting this
       to 3 means that the fourth unsuccessful try will lock the user's
       account -->
  <param name="FailedAttemptsBeforeLockout" value="3"/>
  <!-- Time in milliseconds before a user can retry after an unsuccessful login
       attempt -->
  <param name="LoginRetryDelay" value="0"/>
  <!-- Time in seconds that a user's account will stay locked for, or -1 if it
       must be manually unlocked. -->
  <param name="LockoutPeriod" value="5"/>
  <!-- Whether or not use the ACL permission model.  If false, a user's
       privileges will apply to every claim. If true, access will be controlled
       by the ClaimAccess table. -->
  <param name="UseACLPermissions" value="true"/>


  <!-- Maximum number of users and groups to directly include in search query that checks the claim access control list;
       beyond this limit the users and groups are stored in database tables and are checked via an additional join in the
       query. A value of -1 means use the appropriate default value for the current database -->
  <param name="MaxACLParameters" value="-1"/>

  <!-- If UseACLPermissions is true, setting this parameter to true means that
       supervisors will inherit permissions on a claim that have been added for
       a supervised user or group. -->
  <param name="EnableDownlinePermissions" value="false"/>
  <!-- Whether or not search screens should restrict search results to items
       that the user has permission to view. -->
  <param name="RestrictSearchesToPermittedItems" value="true"/>

  <!-- Whether or not contact potential match screens should restrict match results to items that the user
       has permission to view. -->
  <!--param name="RestrictContactPotentialMatchToPermittedItems" value="true"/-->

  <!-- True if we should synch user roles for LDAP authentication. -->
  <!--param name="ShouldSynchUserRolesInLDAP" value="false"/-->

  <!-- ==============================================================================
    Clustering Parameters
  ==============================================================================
   Is clustering enabled? -->
  <param env="local" name="ClusteringEnabled" value="false"/>
  <param env="dev" name="ClusteringEnabled" value="false"/>
  <param env="int" name="ClusteringEnabled" value="false"/>
  <param env="cert" name="ClusteringEnabled" value="true"/>
  <param env="uat" name="ClusteringEnabled" value="false"/>
  <param env="prod" name="ClusteringEnabled" value="true"/>
  
  <!-- The IP multicast address used by the cluster. Must be unique within the
    range of the cache time to live parameter. -->
  <param name="ClusterMulticastAddress" value="239.0.0.128"/>
  <!-- The time to live for the multicast packets used for cluster communication.
    For Claim Center the value will nearly always be 1, meaning only
    deliver the packets to the local subnet. Higher time to live values require
    multicast routing to be enabled on any intermediate routers (rare in most IT
    organizations). Also the larger the time to live the more you have to worry
    about allocating unique multicast addresses. -->
  <param name="ClusterMulticastTTL" value="1"/>
  <!-- The port to use for cluster communication -->
  <param name="ClusterMulticastPort" value="45566"/>

  <!-- If clustering is enabled, whether to check this server's configuration against the other
       servers in the cluster. -->
  <!--param name="ConfigVerificationEnabled" value="true"/-->

  <!-- ==============================================================================
    Cache Parameters

    NOTES:
     - To specify type-specific cache parameters, simply add in parameters of
       the form <Entity>CacheSize or <Entity>StaleTimeSeconds, where <Entity> is
       the name of the entity type
  ==============================================================================
  
    Cached range size key generator uses when generating individual keys. This
    cannot be a normal parameter in the database because we need it before the
    database has been initialized (and possibly upgraded) -->
  
  <param name="KeyGeneratorRangeSize" value="100"/>
  <!-- The time between refreshes of the group cache, in seconds -->
  <param name="GroupCacheRefreshIntervalSecs" value="600"/>

  <!-- The time between refreshes of the zone cache, in seconds. -->
  <!--param name="ZoneCacheRefreshIntervalSecs" value="86400"/-->

  <!-- The time between refreshes of the script parameter info cache, in seconds -->
  <param name="ScriptParametersRefreshIntervalSecs" value="600"/>
  <!-- Whether the cache is active. -->
  <param name="CacheActive" value="true"/>

   <!-- Time period (in minutes) when cached items are considered "active",
        i.e. the cache should give higher priority to preserve these items.
        This can be thought of as the period when items are being heavily used,
        e.g. how long a user stays on a page. It should be less than the reaping time.-->
   <param name="GlobalCacheActiveTimeMinutes" value="5"/>

   <!-- Time (in minutes) since last use before cached items are considered eligible for reaping. This can be thought of as the period during which
       an entity is most likely to be re-used. -->
   <param name="GlobalCacheReapingTimeMinutes" value="15"/>

   <!-- The percentage of the heap to allocate to the global cache. -->
   <param name="GlobalCacheSizePercent" value="20"/>

   <!-- The amount of space to allocate to the global cache. If this integer value is specified, it will take precendence over GlobalCacheSizePercent.
   <param name="GlobalCacheSizeMegabytes" value=""/>
   -->

   <!-- Time (in minutes) since last write before cached items are considered stale and eligible for reaping. -->
   <param name="GlobalCacheStaleTimeMinutes" value="60"/>

   <!-- This parameter denotes the period of time (in days), in addition to today, for how long statistics are preserved for historical comparison. -->
   <param name="GlobalCacheStatsRetentionPeriodDays" value="7"/>

   <!-- This parameter denotes a period of time (in minutes) and is used for two purposes: 1) how long to preserve the reason why an item was       evicted, after the event occurred. When a cache miss occurs, the reason is looked up and reported in the CacheInfo page. 2) The period for        which to display statistics on the chart on the CacheInfo page.-->
   <param name="GlobalCacheStatsWindowMinutes" value="30"/>

  <!-- ==============================================================================
   Scheduler / Workflow Parameters
  ==============================================================================
   If true, run exception monitoring rules for idle cases at a separate time -->
  <param name="SeparateIdleClaimExceptionMonitor" value="true"/>
  <!-- Claims that haven't been touched for three days (including edits or exception checks)
    are scheduled again for exception detection -->
  <param name="IdleClaimThresholdDays" value="3"/>
  <!-- Whether the internal scheduler is enabled. -->
  <param name="SchedulerEnabled" value="true"/>

   <!-- The number of days to keep workflow information before they can be deleted. -->
  <param name="WorkflowLogPurgeDaysOld" value="30"/>

  <!-- The number of days to keep workflow logs before they can be deleted. -->
  <param name="WorkflowPurgeDaysOld" value="60"/>

  <!-- Aggregation interval in minutes for workflow timing statistics.
      Mean, std dev., etc statistics for time spent executing workflow steps will
      be reported at this interval.  Use 0 to disable stat recording.-->
  <param name="WorkflowStatsIntervalMins" value="60"/>

  <!-- The maximum number of ProcessHistory entries to consider when producing the the Work Queue History download  -->
   <param name="WorkQueueHistoryMaxDownload" value="10000"/>
    
  <!-- ==============================================================================
    Geocoding related parameters.
  ==============================================================================
   Enable Geocoding UI on Address Book Pages. -->
  <param name="UseGeocodingInAddressBook" value="false"/>
  <!-- Enable Geocoding UI on local CC Pages such as the Assignment and User search/popup pages. -->
  <param name="UseGeocodingInPrimaryApp" value="false"/>
  <!-- The maximum distance allowed when performing a proximity search for the nearest-n contacts/users.           This value is treated as miles by default, but will be treated as kilometers if the system default      unit of distance (as controlled by DrivingDirectionsUseMetricDistances) is set to true.             NOTE THAT IF THIS PARAMETER DIFFERS BETWEEN ContactCenter AND ClaimCenter, SOME                         DISTANCES-RELATED MESSAGES MAY BE DISPLAYED INCORRECTLY. -->

  <!-- By default, applications will use miles/US distances for driving distance/directions;  -->
  <!--     if set to true, applications will use kilometers/metric distances.                 -->
  <!-- NOTE THAT IF THIS PARAMETER DIFFERS BETWEEN ContactCenter AND ClaimCenter, SOME        -->
  <!-- DISTANCES-RELATED MESSAGES MAY BE DISPLAYED INCORRECTLY.                               -->
  <param name="UseMetricDistancesByDefault" value="false"/>

  <!-- The maximum distance allowed when performing a proximity search for the nearest-n contacts/users.      -->
  <!--     This value is treated as miles by default, but will be treated as kilometers if the system default -->
  <!--     unit of distance (as controlled by UseMetricDistancesByDefault) is set to true.                    -->
  <!-- NOTE THAT IF THIS PARAMETER DIFFERS BETWEEN ContactCenter AND ClaimCenter, SOME  -->
  <!--      DISTANCES-RELATED MESSAGES MAY BE DISPLAYED INCORRECTLY.                    -->
  <!-- NOTE THAT THIS PARAMETER HAS NO EFFECT ON EITHER RADIUS ("n miles") SEARCHES OR  -->
  <!--      "WALKING THE GROUP TREE" BASED PROXIMITY ASSIGNMENT                         -->
  <param name="ProximitySearchOrdinalMaxDistance" value="300"/>

  <!-- The maximum number of results to be returned by a proximity search when           -->
  <!--     using a fixed radius (n miles/km).  This is ignored for Ordinal (nearest-n)    -->
  <!--     searches.                                                                     -->
  <!-- This can be overriden in the UI or APIs by setting                                -->
  <!--     ProximitySearchParameters.RadiusSearchMaxResults                              -->
  <param name="ProximityRadiusSearchDefaultMaxResultCount" value="1000"/>
    
  <!--
  ==============================================================================
   Spellcheck Parameters
  ==============================================================================
   If true, stepping off a spellcheck-enabled field after making any change
        will invoke the spell checker for the field -->
  <param name="CheckSpellingOnChange" value="false"/>
  <!-- If true, any screen containing editable spellcheck-enabled fields will
       have a spell check button in the the toolbar -->
  <param name="CheckSpellingOnDemand" value="false"/>
  <!-- ==============================================================================
    Business Calendar Parameters
  ==============================================================================
   What time the business day starts at. -->
  <param name="BusinessDayStart" value="8:00 AM"/>
  <!-- What time the business day ends at. -->
  <param name="BusinessDayEnd" value="5:00 PM"/>
  <!-- The day of the week considered the end of the business week. -->
  <param name="BusinessWeekEnd" value="Friday"/>
  <!-- Whether Sunday is considered a business day. -->
  <param name="IsSundayBusinessDay" value="false"/>
  <!-- Whether Monday is considered a business day. -->
  <param name="IsMondayBusinessDay" value="true"/>
  <!-- Whether Tuesday is considered a business day. -->
  <param name="IsTuesdayBusinessDay" value="true"/>
  <!-- Whether Wednesday is considered a business day. -->
  <param name="IsWednesdayBusinessDay" value="true"/>
  <!-- Whether Thursday is considered a business day. -->
  <param name="IsThursdayBusinessDay" value="true"/>
  <!-- Whether Friday is considered a business day. -->
  <param name="IsFridayBusinessDay" value="true"/>
  <!-- Whether Saturday is considered a business day. -->
  <param name="IsSaturdayBusinessDay" value="false"/>
  <!-- A comma-delimited list of dates that should be considered holidays. -->
  <param name="HolidayList" value="1/1/2007, 12/25/2007"/>
  <!-- ==============================================================================
    Search Parameters
  ==============================================================================
   Maximum number of results to return from a duplicate contact search -->
  <param name="MaxDuplicateContactSearchResults" value="25"/>
  <!-- The maximum number of search results before the UI warning appears -->
  <param name="MaxClaimSearchResults" value="300"/>
  <!-- The maximum number of search results before the UI warning appears -->
  <param name="MaxActivitySearchResults" value="300"/>

  <!-- The max number of bulk invoice search results to display in the UI. -->
  <param name="MaxBulkInvoiceSearchResults" value="300"/>

  <!-- The maximum number of search results before the UI warning appears -->
  <param name="MaxCheckSearchResults" value="300"/>
  <!-- The maximum number of search results before the UI warning appears -->
  <param name="MaxRecoverySearchResults" value="300"/>
  <!-- The maximum number of search results before the UI warning appears -->
  <param name="MaxContactSearchResults" value="300"/>
  <!-- The maximum number of policy search results before the UI warning appears -->
  <param name="MaxPolicySearchResults" value="50"/>
  <!-- The maximum number of note search results before the UI warning appears.
    A value of 0 means don't limit the search. -->
  <param name="MaxNoteSearchResults" value="0"/>
  <!-- The maximum number of doc template search results before the UI warning appears -->
  <param name="MaxDocTemplateSearchResults" value="200"/>
  <!-- ClaimCenter works around Oracle bug 5886252 by disabling index fast full scan when executing
       certain claim searches on Oracle. This parameter controls the work around and is true by default.
       If a future version of Oracle fixes the defect this parameter may be removed.
       The parameter has no effect on databases other than Oracle. -->
  <param name="DisableIndexFastFullScanForClaimSearch" value="true"/>
  <!-- ClaimCenter works around Oracle bug 5886252 by disabling index fast full scan when executing
       certain recovery searches on Oracle. This parameter controls the work around and is true by default.
       If a future version of Oracle fixes the defect this parameter may be removed.
       The parameter has no effect on databases other than Oracle. -->
  <param name="DisableIndexFastFullScanForRecoverySearch" value="true"/>
  <!-- ClaimCenter works around Oracle bug 6990305 by disabling optimizer cost base transformation
  when executing certain claim searches on Oracle. This parameter along with DisableIndexFastFullScanForClaimSearch
  controls the work around and is true by default. If a future version of Oracle fixes the defect this parameter may be removed.
  The parameter has no effect on databases other than Oracle -->
  <param name="DisableCBQTForClaimSearch" value="true"/>
  <!-- ClaimCenter works around hash join related query plan problems when executing certain
    claim searches on Oracle. This parameter controls part of the work around and is true by default.
    The parameter has no effect on databases other than Oracle -->
  <param name="DisableHashJoinForClaimSearch" value="true"/>
  <!-- ClaimCenter works around sort merge join query plan problems when executing certain
    claim searches on Oracle. This parameter controls part of the workaround when DisableHashJoinForClaimSearch
    is set to true and is true by default. The parameter has no effect on databases other than Oracle -->
  <param name="DisableSortMergeJoinForClaimSearch" value="true"/>

  <!--
  ==============================================================================
    Claim Health indicator and metric parameters.
  ==============================================================================
  -->
  <!--
     Maximum number of claims per invocation of the Claim Health Calculation batch process
     to calculate metrics and indicators. This parameter is strictly to limit the number of
     claims that can be processed at a single time. The Claim Health Calculation batch process
     calculates the claim metrics and indicators for found claims.
  -->
  <param name="MaxClaimResultsPerClaimHealthCalcBatch" value="1000"/>

  <!--
     Maximum number of years to look back to find claims to calculate metrics and indicators for.
     This parameter is strictly to limit the number of claims found for the Claim Health Calculation
     batch process.
  -->
  <param name="ClaimHealthCalcMaxLossDateInYears" value="2"/>

  <!--
       Number of days new initial reserves contribute to the initial reserve sum of the Percent
       Reserve Change Claim Metric calculation after the first initial reserve is created.  Initial
       reserves are reserves created when creating the Claim/Exposure or the first set of reserves
       created on the Claim/Exposure when there are no previous reserves for those entities.
    -->
  <param name="InitialReserveAllowedPeriod" value="3"/>

  <!--
         Proximity search works around some bad execution plans by disabling hash joins when executing
         certain searches on Oracle. This parameter controls the work around and is false by default.
         If a future version of Oracle fixes the defect this parameter may be removed.
         The parameter has no effect on databases other than Oracle.
  -->
  <param name="DisableHashJoinForProximitySearch" value="false"/>

  <!--
         Proximity search works around some bad execution plans by disabling hash joins when executing
         certain searches on Oracle. This parameter controls the work around and is false by default.
         If a future version of Oracle fixes the defect this parameter may be removed.
         The parameter has no effect on databases other than Oracle.
  -->
  <param name="DisableIndexFastFullScanForProximitySearch" value="false"/>

  <!--
  ==============================================================================
    Claim Catastrophe parameters.
  ==============================================================================
  -->
  <!--
     Maximum number of claims that can be found related to a single catastrophe. This
     parameter is strictly for limiting the number of claims that can be found to match
     a single catastrophe for the Catastrophe Related batch process.
  -->
  <param name="MaxCatastropheClaimFinderSearchResults" value="1000"/>

  <!-- ClaimCenter works around semi join query plan problems by forcing nested loop semi join
     instead of choose when executing certain claim searches on Oracle. This parameter controls
     part of the work around and is true by default. The parameter has no effect on databases other than Oracle -->
  <param name="SetSemiJoinNestedLoopsForClaimSearch" value="true"/>
  <!-- ClaimCenter works around index fast full scan related query plan problems when executing the team
     group activities page's main query on Oracle. This parameter controls the work around and is true by default.
     If a future version of Oracle fixes the defect this parameter may be removed. The parameter has no effect
     on databases other than Oracle. -->
  <param name="DisableIndexFastFullScanForTeamGroupActivities" value="true"/>
  <!-- ClaimCenter works around optimizer cost base transformation related query plan problems when executing
     the team group activities page's main query on Oracle. This parameter controls the work around and is true
     by default. If a future version of Oracle fixes the defect this parameter may be removed. The parameter
     has no effect on databases other than Oracle. -->
  <param name="DisableCBQTForTeamGroupActivities" value="true"/>
  <!-- ClaimCenter works around hash join related query plan problems when executing the team group activities
     page's main query on Oracle. This parameter controls part of the work around and is true by default.
     The parameter has no effect on databases other than Oracle. -->
  <param name="DisableHashJoinForTeamGroupActivities" value="true"/>
  <!-- ClaimCenter works around sort merge join query plan problems when executing the team group activities
     page's main query on Oracle. This parameter controls part of the workaround when DisableHashJoinForClaimSearch
     is set to true and is true by default. The parameter has no effect on databases other than Oracle. -->
  <param name="DisableSortMergeJoinForTeamGroupActivities" value="true"/>
  <!-- ==============================================================================
    Approval Parameters
  ==============================================================================
   The name of the acitivity pattern to use when creating reserve approval activities -->
  <param name="ReserveApprovalPattern" value="approve_reserve_change"/>
  <!-- The name of the activity pattern to use when creating payment approval activities -->
  <param name="PaymentApprovalPattern" value="approve_payment"/>
  <!-- The name of the acitivity pattern to use when creating recovery approval activities -->
  <param name="RecoveryApprovalPattern" value="approve_recovery"/>
  <!-- The name of the activity pattern to use when creating recovery reserve approval activities -->
  <param name="RecoveryReserveApprovalPattern" value="approve_recoveryreserve_change"/>
  <!-- The name of the acitivity pattern to use when creating bulk invoice approval activities -->
  <param name="BulkInvoiceApprovalPattern" value="approve_bulkinvoice"/>

  <!--
  ==============================================================================
    Misc Bulk Invoice Activity Pattern Parameters
  ==============================================================================
  -->

  <!-- The name of the activity pattern to use when creating an alert activity regarding the failed processing of a bulk invoice item -->
  <param name="BulkInvoiceItemValidationFailedPattern" value="bulkinvitem_validation_failed"/>

  <!-- The name of the activity pattern to use when creating an alert activity regarding the failed void of a bulk invoice -->
  <param name="BulkInvoiceUnableToVoidPattern" value="unable_to_void_bulkinvoice"/>

  <!-- The name of the activity pattern to use when creating an alert activity regarding the failed stop of a bulk invoice -->
  <param name="BulkInvoiceUnableToStopPattern" value="unable_to_stop_bulkinvoice"/>

  <!--
  ==============================================================================
    Misc Financials Activity Patterns
  ==============================================================================
  -->

  <!-- The name of the activity pattern to use when creating an alert activity regarding the downstream denial of a check -->
  <param name="CheckDeniedPattern" value="check_denied"/>

  <!-- The name of the activity pattern to use when creating an alert activity regarding the downstream denial of a recovery -->
  <param name="RecoveryDeniedPattern" value="recovery_denied"/>

  <!-- The name of the activity pattern to use when a check cannot be stopped -->
  <param name="CheckUnableToStopPattern" value="unable_to_stop_check"/>

  <!-- The name of the activity pattern to use when a check cannot be voided -->
  <param name="CheckUnableToVoidPattern" value="unable_to_void_check"/>

  <!-- The name of the activity pattern to use when the last payment of recurrence checks is coming up -->
  <param name="LastPaymentReminderPattern" value="last_payment_reminder"/>

  <!-- ==============================================================================
    Segnmentation Parameters
  ==============================================================================
   The default value to set the Segment field to on a Claim -->
  <param name="ClaimSegment" value="unknown"/>
  <!-- The default value to set the Strategy field to on a Claim -->
  <param name="ClaimStrategy" value="unknown"/>
  <!-- The default value to set the Segment field to on an Exposure -->
  <param name="ExposureSegment" value="unknown"/>
  <!-- The default value to set the Strategy field to on an Exposure -->
  <param name="ExposureStrategy" value="unknown"/>
  <!-- ==============================================================================
    Assignment Parameters
  ==============================================================================
   Whether or not to display the UI portions of the assignment queue mechanism. Once turned on it can not be turned off again -->
  <param name="AssignmentQueuesEnabled" value="false"/>
  <!-- ==============================================================================
    Deduction Parameters
  ==============================================================================
   The typecode (in DeductionType) that identifies backup withholding -->
  <param name="BackupWithholdingTypeCode" value="irs"/>
  <!-- The standard backup withholding rate, as defined by the IRS.
    The number is a percentage (e.g., 28.0 means 28.0 percent). -->
  <param name="StandardWithholdingRate" value="28.0"/>
  <!-- Indicates whether backup withholding should be calculated for checks. -->
  <param name="CalculateBackupWithholdingDeduction" value="false"/>
  <!-- ==============================================================================
    Statistics, Team and Dashboard Parameters
  ==============================================================================
   Whether or not to show individual user global workload stats along side
       normal stats in Team - Summary list view. -->
  <param name="GroupSummaryShowUserGlobalWorkloadStats" value="true"/>
  <!-- Sets the time window used to calculate user statistics. Possible values are:
    0 (default) - Use "This Week" as the window, defined as the start of the current business week until now
    -1 - Use "This month", defined as the start of the current month until now
    Any positive integer N - The window is the last N days of activity, including the current day
    NOTE: If this property is changed, then you should probably also use display_ext.properties to override
    the statistics display keys which refer to "this week"; see the NVV.Admin.UserStatistics.Statistics
    section of display.properties -->
  <param name="UserStatisticsWindowSize" value="0"/>
  <!-- The number of days to use when calculating the first claim aging bucket.  The bucket will include claims
    between 0 and AgingStatsFirstDivision days old. -->
  <param name="AgingStatsFirstDivision" value="30"/>
  <!-- The number of days to use when calculating the second claim aging bucket.  The bucket will include claims
    between AgingStatsFirstDivision + 1 and AgingStatsSecondDivision days old. -->
  <param name="AgingStatsSecondDivision" value="60"/>
  <!-- The number of days to use when calculating the fourth claim aging bucket.  The bucket will include claims
    between AgingStatsSecondDivision + 1 and AgingStatsThirdDivision days old.  In addition, the last bucket
    will include all claims older than AgingStatsThirdDivision days. -->
  <param name="AgingStatsThirdDivision" value="120"/>
  <!-- Whether or not to display the number of litigated claims on the claim aging screens. -->
  <param name="CalculateLitigatedClaimAgingStats" value="true"/>
  <!-- Determines the number of days used for executive dashboard calculations that depends on a period. -->
  <param name="DashboardWindowPeriod" value="30"/>
  <!-- The total incurred amount above which the executive dashboard will count the claim as over limit. -->
  <param name="DashboardIncurredLimit" value="1000000"/>
  <!-- Whether or not to display the coverage stats on the executive dashboard. -->
  <param name="DashboardShowByCoverage" value="true"/>
  <!-- Whether or not to display the line of business and loss type stats on the executive dashboard. -->
  <param name="DashboardShowByLineOrLoss" value="true"/>
  <!-- ==============================================================================
    Financial Parameters
  ==============================================================================
   What the Financials version is -->
  <param name="Financials" value="entry"/>
  <!-- Whether to use the Recovery Reserve transacti in financials -->
  <param name="UseRecoveryReserves" value="true"/>
  <!-- Whether to allow multiple line items in a transaction -->
  <param name="AllowMultipleLineItems" value="true"/>
  <!-- Whether to allow multiple payments per check -->
  <param name="AllowMultiplePayments" value="true"/>
  <!-- What level to zero reserves at (0 = costtype, 1 = costcategory) -->
  <param name="ZeroReservesLevel" value="0"/>
  <!-- Configure the Reserves UI to allow the user to modify Reserves by setting Total Incurred (true), or by
       setting Available Reserves (false), which is the default -->
  <param name="SetReservesByTotalIncurred" value="false"/>
  <!-- Always check authority limits when approving a TransactionSet -->
  <param name="CheckAuthorityLimits" value="false"/>
  <!-- If true, users can submit payments that exceed available reserves up to the
       amount limited by the PaymentsExceedReserves authority limits. Otherwise,
       no partial or final payments that exceed reserves are allowed (other than
       first & final payments). -->
  <param name="AllowPaymentsExceedReservesLimits" value="true"/>
  <!-- If true, user is allowed to create supplemental payments on closed claim or exposure with no prior payments.
       else such payments would be blocked. -->
  <param name="AllowNoPriorPaymentSupplement" value="true" />
  <!-- Payments above this threshold will be logged -->
  <param name="PaymentLogThreshold" value="100"/> <!-- GAIC change to 100 from 500 -->
  <!-- Whether to close an exposure after a relevant final payment is made -->
  <param name="CloseExposureAfterFinalPayment" value="true"/>
  <!-- Whether to close a claim if a final payment closes last open exposure -->
  <param name="CloseClaimAfterFinalPayment" value="true"/>
  <!--
  ==============================================================================
    Integration Parameters
  ==============================================================================
  -->
  <!-- If true, the Metro integration is enabled from UI and will send messages to the Metropolitan Reporting Bureau service -->
  <param name="EnableMetroIntegration" value="false"/>
  <!-- The name of the Metro properties file in the Metro configuration directory" -->
  <param name="MetroPropertiesFileName" value="Metro.properties"/>

  <!-- The name of the ISO properties file in the ISO configuration directory" --> 
  <param name="ISOPropertiesFileName" value="ISO_LOCAL.properties" env="local"/>
  <param name="ISOPropertiesFileName" value="ISO.properties" env="prod" />
  <param name="ISOPropertiesFileName" value="ISO_Dev.properties" env="dev" />
  <param name="ISOPropertiesFileName" value="ISO_Dev2.properties" env="dev2" />
  <param name="ISOPropertiesFileName" value="ISO_Dev3.properties" env="dev3" />
  <param name="ISOPropertiesFileName" value="ISO_Dev4.properties" env="dev4" />
  <param name="ISOPropertiesFileName" value="ISO_Dev5.properties" env="dev5" />
  <param name="ISOPropertiesFileName" value="ISO_Dev6.properties" env="dev6" />
  <param name="ISOPropertiesFileName" value="ISO_Dev7.properties" env="dev7" />
  <param name="ISOPropertiesFileName" value="ISO_Dev8.properties" env="dev8" />
  <param name="ISOPropertiesFileName" value="ISO_Dev9.properties" env="dev9" />
  <param name="ISOPropertiesFileName" value="ISO_INT.properties" env="int" />
  <param name="ISOPropertiesFileName" value="ISO_INT2.properties" env="int2" />
  <param name="ISOPropertiesFileName" value="ISO_INT3.properties" env="int3" />
  <param name="ISOPropertiesFileName" value="ISO_UAT.properties" env="uat" />
  <param name="ISOPropertiesFileName" value="ISO_CERT.properties" env="cert" />

  <!-- Activate the Contact Autosync worker automatically when autosync events are received.
       Defaults to true; if set to false, Contacts will only be synchronized when the Autosync
       writer batch job runs. -->
 <param name="InstantaneousContactAutoSync" value="true"/>

  <!-- Old messages in the message history table are purged when they have been there for this
       number of days -->
  <!--param name="KeepCompletedMessagesForDays" value="90"/-->

  <!-- ==============================================================================
    UI Parameters
  ==============================================================================
   If enabled, forces exposures to follow the claimant/coverage uniqueness constraint. -->
  <!-- The Look & Feel theme for ClaimCenter -->  
  <param name="UISkin" value="Ocean"/>  
  <param name="EnableClaimantCoverageUniquenessConstraint" value="false"/>
  <!-- This option allows hiding the NewExposure-action-menu for specific LossTypes.  (case-sensitive)
       Note that removing this menu item for a LossType will also hide the Exposures step in the New Claim Wizard for that LossType. -->
  <param name="ShowNewExposureMenuForLossTypes" value="AUTO,PR,GL,EQUINE,AGRIPROPERTY,AGRILIABILITY,AGRIAUTO,PIMINMARINE,FIDCRIME,EXECLIABDIV,EXCESSLIABILITY,EXCESSLIABILITYAUTO,PROFLIABDIV,AGRIXSUMBAUTO,AGRIXSUMBLIAB,COMMBONDS,SPECIALTYES,KIDNAPRANSOM,ENVLIAB,OMAVALON,PERSONALAUTO,AVIATION,MERGACQU,SPECIALHUMSERV,ALTMARKETSAUTO,SHSAUTO,TRUCKINGAUTO"/>
  <!-- Works with ShowNewExposureMenuForLossTypes; claims that do not have the new exposure menu have
       a fixed list of exposures which can be shown either via tabs on the claim loss page (if this
       option is true) or via separate top level page links in the claim file (if option is false) -->
  <param name="ShowFixedExposuresInLossDetails" value="false"/>
  <!-- NewExposure-action-menu can have two sub-menus.  This option allows hiding the ChooseByCoverageType submenu
    for specific LossTypes. (case-sensitive) -->
  <param name="ShowNewExposureChooseByCoverageTypeMenuForLossTypes" value="AUTO,PR,GL,WC"/>
  <!-- NewExposure-action-menu has two sub-menus.  This option allows hiding the ChooseByCoverage submenu
    for specific LossTypes. (case-sensitive). Recommendation: Omit WC from this list. -->
  <param name="ShowNewExposureChooseByCoverageMenuForLossTypes" value="AUTO,PR,GL,EQUINE,AGRIPROPERTY,AGRILIABILITY,AGRIAUTO,PIMINMARINE,FIDCRIME,EXECLIABDIV,EXCESSLIABILITY,EXCESSLIABILITYAUTO,PROFLIABDIV,AGRIXSUMBAUTO,AGRIXSUMBLIAB,COMMBONDS,SPECIALTYES,KIDNAPRANSOM,ENVLIAB,OMAVALON,PERSONALAUTO,PIMINMARINEWC,PIMINMARINEEL,SPECIALTYESWC,SPECIALTYESEL,ALTMARKETSWC,ALTMARKETSEL,STRATEGICCOMPWC,STRATEGICCOMPEL,TRUCKINGWC,TRUCKINGEL,AGRIWC,AGRIEL,OMWC,OMEL,ECUWC,ECUEL,AVIATION,MERGACQU,SPECIALHUMSERV,ALTMARKETSAUTO,SHSAUTO,TRUCKINGAUTO"/>
  <!-- NewExposure-action-menu/ChooseByCoverage has three sub-menus.  This option allows hiding empty Vehicle and Property
    submenus for specific LossTypes.  (case-sensitive) -->
  <param name="HidePolicyObjectsWithNoCoveragesForLossTypes" value="EQUINE,AGRIPROPERTY,AGRILIABILITY,AGRIAUTO,PIMINMARINE,FIDCRIME,EXECLIABDIV,EXCESSLIABILITY,EXCESSLIABILITYAUTO,PROFLIABDIV,AGRIXSUMBAUTO,AGRIXSUMBLIAB,COMMBONDS,SPECIALTYES,KIDNAPRANSOM,ENVLIAB,OMAVALON,PERSONALAUTO,PIMINMARINEWC,PIMINMARINEEL,SPECIALTYESWC,SPECIALTYESEL,ALTMARKETSWC,ALTMARKETSEL,STRATEGICCOMPWC,STRATEGICCOMPEL,TRUCKINGWC,TRUCKINGEL,AGRIWC,AGRIEL,OMWC,OMEL,ECUWC,ECUEL,AVIATION,MERGACQU,SPECIALHUMSERV,ALTMARKETSAUTO,SHSAUTO,TRUCKINGAUTO"/>
  <!-- Limits the number of vehicle submenus available on the NewExposure-action-menu/ChooseByCoverage
  submenu. If this limit is exceeded users will be asked to use the coverage type menu instead -->
  <param name="MaxChooseByCoverageMenuItems" value="15"/>
  <!-- Limits the number of type submenus available on the NewExposure-action-menu/ChooseByCoverage
  type submenu. If this limit is exceeded the coverage types will be split into alphabetic sub-menus -->
  <param name="MaxChooseByCoverageTypeMenuItems" value="15"/>
  <!-- The maximum number of users to display in the chart on the Team Summary page
           Set this to 0 to remove the chart entirely. Otherwise, the chart will display
           statistics for the top N users, and group the others into a bar labeled "All Other Users" -->
  <param name="MaxTeamSummaryChartUserBars" value="10"/>
  <!-- Defines the set of reopenable entities for which, when reopened, a screen should be displayed
   for the user to enter a reason and note -->
  <param name="RequestReopenExplanationForTypes" value="Claim,Exposure,Matter"/>
  <!-- If true, the policy vehicles screens will suppress the message telling the user whether all of the vehicles
    which appear on the policy have been downloaded to the ClaimCenter policy snapshot. This should be set to true
    if the policy adapter is not capable of returning a meaningful value for policy.TotalVehicles,
    and generally set to false otherwise. -->
  <param name="IgnorePolicyTotalVehiclesValue" value="false"/>
  <!-- If true, the policy properties screens will suppress the message telling the user whether all of the vehicles
    which appear on the policy have been downloaded to the ClaimCenter policy snapshot. This should be set to true
    if the policy adapter is not capable of returning a meaningful value for policy.TotalProperties,
    and generally set to false otherwise. -->
  <param name="IgnorePolicyTotalPropertiesValue" value="false"/>
  <!-- The number of entries to display per page in a ListView, where not explicitly specified. -->
  <param name="ListViewPageSizeDefault" value="15"/>
  <!-- The character used as a placeholder in masked input fields -->
  <param name="InputMaskPlaceholderCharacter" value="."/>
  <!-- The maximum number of browser history items to keep. Set this to 0 to disable the Back button functionality entirely. -->
  <param name="MaxBrowserHistoryItems" value="0"/>
  <!-- The keyboard shortcut to activate the QuickJump box. -->
  <param name="QuickJumpShortcut" value="/"/>
  <!-- Time in seconds that a treeview state will be cached. -->
  <param name="TreeViewRefresh" value="120"/>
  <!-- ==============================================================================
    Misc. Parameters
  ==============================================================================
   Whether to create snapshots for imported and created claims -->
  <!--
    The soap webservices uses finite namespace - something is either in the api, entity, fault, or enumeration namespace.
    This quite often leads to conflicts/overwriting of classes in that namespace.  This prevents you from having duplicate
    webservice class names.  But for referenced classes this is not an issue unless you call these soap webservices via
    soap.local, or generate the clients clients in the same directory.
    So to turn off the warning in studio change this to true. -->
  <param name="AllowSoapWebServiceReferenceNamespaceCollisions" value="false"/>
  <!--
    The soap webservices bundle was setting ignore bean version conflicts, several people have pointed out that this
    is quite dangerous. So we have changed the default behavior, and added an annotation to get back to that behavior,
    however we do not want to disrupt current customer in a minor version upgrade so the new behavior is dependent
    on setting this to true.
   -->
  <param name="UseSafeBundleForWebServiceOperations" value="true"/>
  <param name="EnableClaimSnapshot" value="true"/>
  <!-- Whether to enable statistical coding support -->
  <param name="EnableStatCoding" value="false"/>
  <!-- Cap on number of stat codes to show in stat code picker dropdown -->
  <param name="MaxStatCodesInDropdown" value="20"/>
  <!-- Whether to enable automatic claim number generation (via the adapter) -->
  <param name="EnableClaimNumberGeneration" value="true"/>
  <!-- External entity collection types will be exposed as arrays to support
       existing java plugin implementations without any code upgrade. -->
  <param name="LegacyExternalEntityArraySupport" value="false"/>
   <!-- If true, when the policy for a claim is refreshed or changed (through the Refresh/Select Policy buttons in the UI),
      the new policy will be validated at the level of the old policy. If false, the new policy will be validated at
      newloss. In either case, a validation failure will cause the policy refresh or change to be reverted-->
  <param name="MaintainPolicyValidationLevelOnPolicyChange" value="true"/>
  <!-- ==============================================================================
    PDF Print Setting Parameters

    NOTES:
     - By default, output will be to standard 8.5 x 11" paper in landscape mode.
       To switch to portrait mode printing, simply swap the PrintPageHeight and
       PrintPageWidth values.
  ==============================================================================
   Total height of a printed page -->
  <param name="PrintPageHeight" value="8.5in"/>
  <!-- Total width of a printed page -->
  <param name="PrintPageWidth" value="11in"/>
  <!-- Height of the top margin of a printed page -->
  <param name="PrintMarginTop" value="0.5in"/>
  <!-- Height of the bottom margin of a printed page -->
  <param name="PrintMarginBottom" value="0.5in"/>
  <!-- Width of the left margin of a printed page -->
  <param name="PrintMarginLeft" value="1in"/>
  <!-- Order that validation should execute when validating multiple entities.  Any entities not specified in this list will be validated after all listed entities -->
  <param name="EntityValidationOrder" value="Policy,Claim,Exposure,Matter,TransactionSet"/>
  <!-- Width of the right margin of a printed page -->
  <param name="PrintMarginRight" value="1in"/>
  <!-- Noraml print text height -->
  <param name="PrintFontSize" value="10pt"/>
  <!-- Font size of text within a list view -->
  <param name="PrintListViewFontSize" value="10pt"/>
  <!-- Print text height of section headers -->
  <param name="PrintHeaderFontSize" value="16pt"/>
  <!-- Total height of a line in print -->
  <param name="PrintLineHeight" value="14pt"/>
  <!-- Name of font family to use for output. -->
  <param name="PrintFontFamilyName" value="sans-serif"/>
  <!-- Optional fully qualified path to a valid FOP user configuration file. Use this to
       specify or override the default FOP configuration. -->
  <param name="PrintFOPUserConfigFile" value=""/>
  <!-- The Content-Disposition setting to use when the content to be printed is returned
       to the browser. Must be either 'attachment' (the default) or 'inline' -->
  <param name="DefaultContentDispositionMode" value="attachment"/>
  <param name="PrintMaxPDFInputFileSize" value="50"/>
  <!-- ==============================================================================
    Document Creation and Document Management Parameters
  ==============================================================================
   Set this parameter to false to completely disable the Guidewire Document Assistant
        ActiveX control -->
  <param name="AllowActiveX" value="true"/>
  <!-- Whether the Guidewire Document Assistant ActiveX control be used to display document contents. -->
  <param name="UseGuidewireActiveXControlToDisplayDocuments" value="true"/>
  <!-- Whether the Documents list should display Edit/Upload buttons.
         Set this to false if the IDocumentContentSource integration mechanism doesn't support them.
         Must be set to false if AllowActiveX is false. -->
  <param name="DisplayDocumentEditUploadButtons" value="false"/>
  <!-- The Content-Disposition setting to use when document content is returned
       directly to the browser (when AllowActiveX is set to false. Must be either 'inline' (the default) or
       'attachment' -->
  <param name="DocumentContentDispositionMode" value="inline"/>
  <!-- Location of the XSD file to be used to validate Doc Template Descriptor xml files -->
  <param name="DocumentTemplateDescriptorXSDLocation" value="gaic-document-template.xsd"/>
  <!-- This flag indicates that if a Document has been rolledback, that on commit both the isDocument and addDocument should be called again -->
  <param name="ReCallDocumentContentSourceAfterRollback" value="false"/>

<!-- The maximum allowable file size (in megabytes) which may be uploaded.
        Any attempt to upload a file larger than this will fail.
        Since the uploaded document must be handled on the server,
        this parameter protects the server from possible memory consumption problems. -->
  <param name="MaximumFileUploadSize" value="20"/>
  
  <!-- Whether to delete the Approval history when the check is edited -->
  <param name="DeleteCompletedApprovalActivitiesOnEdit" value="false"/>
  <!-- ==============================================================================
    Mime Type Information

    This section tells the system how to map between mime types, file
    extensions, and icons in the UI
  ============================================================================== -->
  <mimetypemapping>
    <mimetype name="application/msword" extensions=".doc" icon="word.gif"
              description="Microsoft Word Document"/>
    <mimetype name="application/vnd.ms-excel" extensions=".xls"
              icon="excel.gif" description="Microsoft Excel Worksheet"/>
    <mimetype name="application/x-msexcel" extensions=".xls" icon="excel.gif"
              description="Microsoft Excel Worksheet (Windows 98)"/>
    <!-- ivorobyeva (06-17-2015): Added .mpw for HP Empower documents -->
    <mimetype name="application/mpw" extensions=".mpw" icon="empower.gif"
              description="HP Empower Document"/>
    <mimetype name="application/mpwf" extensions=".mpwf" icon="acrobat.gif"
              description="Finalized HP Empower Document"/>
    <mimetype name="application/pdf" extensions=".pdf" icon="acrobat.gif"
              description="PDF File"/>
    <mimetype name="text/plain" extensions=".txt" icon="document.gif"
              description="Text Document"/>
    <mimetype name="text/html" extensions=".html|.htm" icon="document.gif"
              description="HTML File"/>
    <mimetype name="text/xml" extensions=".xml" icon="document.gif"
              description="XML Document"/>
    <mimetype name="image/bmp" extensions=".bmp" icon="document.gif"
              description="Bitmap Image"/>
    <mimetype name="image/jpeg" extensions=".jpg" icon="document.gif"
              description="JPEG Image"/>
    <mimetype name="image/pjpeg" extensions=".jpg" icon="document.gif"
              description="JPEG Image"/>
    <mimetype name="application/rtf" extensions=".rtf" icon="word.gif"
              description="Rich Text Format"/>
    <mimetype name="application/csv" extensions=".csv" icon="excel.gif"
              description="Microsoft Office Excel Comma Separated Values File"/>
    <mimetype name="image/tiff" extensions=".tiff|.tif" icon="document.gif"
              description="Microsoft Office Document Imaging File"/>
    <mimetype name="application/octet-stream" extensions="" icon="document.gif"
              description="Unrecognized File Format"/>
    <mimetype extensions=".docx" name="application/vnd.openxmlformats-officedocument.wordprocessingml.document" icon="word.gif"
              description="open xml word"/>
    <mimetype extensions=".xlsx" name="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet" icon="excel.gif"
              description="open xml excel"/>
    <mimetype extensions=".ps" name="application/postscript" icon="document.gif"
              description="Postscript"/>
    <mimetype extensions=".rtx" name="text/richtext" icon="document.gif"
              description="Rich text"/>
    <mimetype extensions=".wav" name="audio/wav" icon="document.gif"
              description="Wave audio"/>
    <mimetype extensions=".wma" name="audio/x-ms-wma" icon="document.gif"
              description="Microsoft audio"/>
    <mimetype extensions=".mdi" name="image/vnd.ms-modi" icon="document.gif"
              description="Microsoft modi"/>
    <mimetype extensions=".gif" name="image/gif" icon="document.gif"
              description="gif image"/>
    <mimetype extensions=".png" name="image/x-png" icon="document.gif"
              description="png image"/>
    <mimetype extensions=".mov" name="video/quicktime" icon="document.gif"
              description="QuickTime video"/>
    <mimetype extensions=".mpg|.mpeg" name="video/mpeg" icon="document.gif"
              description="mpeg video"/>
    <mimetype extensions=".avi" name="video/x-msvideo" icon="document.gif"
              description="Microsoft video"/>
    <mimetype extensions=".ppt|.pps" name="application/vnd.ms-powerpoint" icon="document.gif"
              description="Power point"/>
    <mimetype extensions=".pptx" name="application/vnd.openxmlformats-officedocument.presentationml.presentation" icon="document.gif"
              description="Power point"/>
  </mimetypemapping>

  <!-- the default pdf merging is handled by a product that has a license.  This license can be server specific so
  this line may have to be repeated for each server.  -->
  <param name="PDFMergeHandlerLicenseKey" value="" />

  <!--
  ==============================================================================
    Velocity Parameters

    NOTES:
     - In general usage of Velocity is deprecated
     - velocimacro.library.autoreload MUST be set false in production
     - file.resource.loader.modificationCheckInterval SHOULD be set to -1 in
       production (disables reloading the files)
     - resource.manager.defaultcache.size SHOULD be a prime number larger than
       total number of templates
  ============================================================================== -->
  <velocityParam name="file.resource.loader.cache" value="true"/>
  <velocityParam name="file.resource.loader.modificationCheckInterval" value="-1"/>
  <velocityParam name="velocimacro.library" value="cc_VelocityMacros.vm"/>
  <velocityParam name="velocimacro.permissions.allow.inline" value="true"/>
  <velocityParam name="velocimacro.library.autoreload" value="false"/>
  <velocityParam name="velocimacro.permissions.allow.inline.to.replace.global" value="true"/>
  <velocityParam name="resource.manager.defaultcache.size" value="127"/>
</config>
