<?xml version="1.0"?>
<Entity
  name="TrailerExt">
  <Columns>
    <Column
      beanPath="TrailerExt.Year"
      name="Year"
      sortOrder="0"/>
    <Column
      beanPath="TrailerExt.Manufacturer"
      name="Manufacturer"
      sortOrder="1"/>
    <Column
      beanPath="TrailerExt.Model"
      name="Model"
      sortOrder="2"/>
  </Columns>
  <DisplayName><![CDATA[var retString = "";
  	    if(Year==null and Manufacturer==null and Model==null){
	  	  retString = retString + "Trailer"
	    } else {
	      retString = retString + "Trailer: "
	    }
  	  	if(Year!=null){
  	  	  retString = retString + Year + " "
  	  	}
  	  	if(Manufacturer!=null){
  	  	  retString = retString + Manufacturer + " "
  	  	}
  	  	if(Model!=null){
  	  	  retString = retString + Model
  	  	}
  	  return retString;]]></DisplayName>
</Entity>
